name: Build release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: Existing semantic version tag to build, for example v1.2.0
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  build-and-release:
    runs-on: windows-latest
    env:
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

    steps:
      - name: Check out the tagged source
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ env.RELEASE_TAG }}

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 9.0.x

      - name: Validate tag and project version
        shell: pwsh
        run: |
          if ($env:RELEASE_TAG -notmatch '^v(?<version>\d+\.\d+\.\d+)$') {
              throw "Release tag must use vMAJOR.MINOR.PATCH, received '$env:RELEASE_TAG'."
          }

          $tagVersion = $Matches.version
          [xml]$project = Get-Content '.\SuperLighter.App\SuperLighter.App.csproj'
          $projectVersion = $project.SelectSingleNode('//Version').InnerText
          if ($projectVersion -ne $tagVersion) {
              throw "Tag version '$tagVersion' does not match project version '$projectVersion'."
          }

          "RELEASE_VERSION=$tagVersion" >> $env:GITHUB_ENV

      - name: Restore dependencies
        run: dotnet restore .\SuperLighter.sln

      - name: Verify formatting
        run: dotnet format .\SuperLighter.sln --verify-no-changes --no-restore

      - name: Build
        run: dotnet build .\SuperLighter.sln -c Release --no-restore

      - name: Run self-tests
        shell: pwsh
        run: |
          $test = Start-Process `
              -FilePath 'dotnet' `
              -ArgumentList '.\SuperLighter.App\bin\Release\net9.0-windows\SuperLighter.dll', '--self-test' `
              -Wait `
              -PassThru `
              -NoNewWindow
          if ($test.ExitCode -ne 0) {
              throw "Self-tests failed with exit code $($test.ExitCode)."
          }

      - name: Publish standalone executable
        shell: pwsh
        run: |
          .\publish-win-x64.ps1

          $releaseDirectory = Join-Path $PWD 'release-assets'
          New-Item -ItemType Directory -Path $releaseDirectory -Force | Out-Null
          $releaseExecutable = Join-Path $releaseDirectory 'SuperLighter-win-x64.exe'
          Copy-Item `
              -LiteralPath '.\artifacts\publish\win-x64\SuperLighter.exe' `
              -Destination $releaseExecutable

          $hash = (Get-FileHash -LiteralPath $releaseExecutable -Algorithm SHA256).Hash.ToLowerInvariant()
          "$hash  SuperLighter-win-x64.exe" | Set-Content `
              -LiteralPath (Join-Path $releaseDirectory 'SHA256SUMS.txt') `
              -Encoding Ascii

      - name: Create or update GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $repository = $env:GITHUB_REPOSITORY
          $executable = '.\release-assets\SuperLighter-win-x64.exe'
          $checksums = '.\release-assets\SHA256SUMS.txt'

          gh release view $env:RELEASE_TAG --repo $repository *> $null
          if ($LASTEXITCODE -eq 0) {
              gh release upload $env:RELEASE_TAG `
                  $executable `
                  $checksums `
                  --clobber `
                  --repo $repository
          }
          else {
              gh release create $env:RELEASE_TAG `
                  $executable `
                  $checksums `
                  --generate-notes `
                  --latest `
                  --repo $repository `
                  --title "SuperLighter $env:RELEASE_TAG" `
                  --verify-tag
          }
